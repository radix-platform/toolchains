#!/bin/sh

#################################################################
#
# default common variables:
#
if test -z "$HOST"; then
  #export HOST=i586-pc-cygwin
  export HOST=x86_64-pc-linux-gnu
fi

if test -z "$TARGET"; then
  export TARGET=arm-at91sam7s-elf
fi

if test -z "$TOOLCHAIN_PATH"; then
  export TOOLCHAIN_PATH=/opt/toolchain/arm-AT91SAM7S-elf-newlib/1.0.3
fi

#
# END of default common variables.
#
#################################################################
